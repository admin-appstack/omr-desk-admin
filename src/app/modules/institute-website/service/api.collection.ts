import { environment } from '../../../../environments/environment';

export const ENDPOINTS = {
    INSTITUTE_WEBSITE: environment.apiUrl + '/institute-website',
    PAGES: environment.apiUrl + '/institute-website/pages',
};
